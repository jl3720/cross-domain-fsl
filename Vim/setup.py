from setuptools import setup, find_packages

setup(
    name='vim',
    version='0.1.0',
    packages=find_packages(include=["vim"]),
    description='Vision Mamba with vendored dependencies',
    author='James Liu'
)